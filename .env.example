# ──────────────────────────────────────
# RemoteOps Suite v2 — Environment
# ──────────────────────────────────────

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=remoteos
POSTGRES_USER=remoteos
POSTGRES_PASSWORD=remoteos_dev_2026

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Server
SERVER_PORT=3100
JWT_SECRET=change-me-in-production-super-secret-key
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=604800

# Agent
AGENT_SERVER_URL=ws://localhost:3100
AGENT_HEARTBEAT_INTERVAL_MS=10000
AGENT_RECONNECT_DELAY_MS=3000
